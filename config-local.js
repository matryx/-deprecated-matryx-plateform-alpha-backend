// To be replace by stating config
module.exports = {
    pg: {
        host: 'localhost', // Dummy local db to test
        port: 5433, // Dummy local db to test
        user: 'leo', // Those are dummy, replace by local DB info
        password: '', // Those are dummy, replace by local DB info
        database: 'matryx-test', // Those are dummy, replace by local DB info
    },
    port: 8081,
};
